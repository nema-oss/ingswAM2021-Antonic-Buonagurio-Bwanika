/* package it.polimi.ingsw;

import org.junit.Test;
import static org.junit.jupiter.api.Assertions.*;

public class FaithPointTest {

    private FaithPoint faithPoint;

   @Test
   public void testUseEffect(){
       Player p1 = new Player("Pippo");
       Player p2 = new Player("Pluto");
       faithPoint = new FaithPoint();
       faithPoint.useEffect(p1);
       p2.getPlayerBoard().getPopeRoad().move(1);
       assertEquals(p1.getPlayerBoard().getPopeRoad().getCurrentPosition(), p2.getPlayerBoard().getPopeRoad().getCurrentPosition(), "useEffect method should move player's position in PopeRoad by 1");

    }
}

*/
